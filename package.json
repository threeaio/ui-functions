{
  "name": "root",
  "private": false,
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.13.0",
    "lerna": "^8.1.9",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "clean": "lerna run clean",
    "install:all": "pnpm install",
    "publish:all": "npx lerna publish"
  }
}
